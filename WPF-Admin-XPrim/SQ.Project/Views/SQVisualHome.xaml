<Page
    x:Class="SQ.Project.Views.SQVisualHome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SQ.Project.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:SQ.Project.ViewModels"
    Title="SQVisualHome"
    d:DataContext="{d:DesignInstance Type=viewModels:SQVisualHomeViewModel}"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="TransparentButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                            <ContentPresenter
                                x:Name="contentPresenter"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource Background.Level3.Brush}" />
                                <Setter TargetName="border" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource Background.Level4.Brush}" />
                                <Setter TargetName="contentPresenter" Property="Opacity" Value="0.6" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.3" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid Margin="2">
        <Grid.Background>
            <ImageBrush ImageSource="/SQ.Project;component/Assets/panel.png" Stretch="Fill" />
        </Grid.Background>

        <Grid Margin="15" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <!--<StackPanel Orientation="Horizontal">
                <TextBlock
                    Margin="10,0"
                    FontSize="30"
                    FontWeight="Bold"
                    Foreground="{DynamicResource SYstem.Main.Color.Fill}">
                    转子线体数据过站信息
                </TextBlock>
            </StackPanel>-->
            <Grid
                Grid.Row="1"
                Width="Auto"
                Height="Auto">
                <Viewbox Stretch="Uniform" StretchDirection="Both">
                    <ItemsControl ItemsSource="{Binding Items}">
                        <ItemsControl.Template>
                            <ControlTemplate>
                                <Grid>
                                    <ItemsPresenter />
                                </Grid>
                            </ControlTemplate>
                        </ItemsControl.Template>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Grid />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                                <Setter Property="VerticalAlignment" Value="Stretch" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid ClipToBounds="True">
                                    <Border
                                        x:Name="ImageContainer"
                                        Background="Transparent"
                                        CornerRadius="4"
                                        Opacity="0">
                                        <Grid Width="1920" Height="900">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height=".2*" />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock
                                                Grid.Row="0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Top"
                                                FontSize="25"
                                                Foreground="{DynamicResource Primary.Brush}"
                                                Text="{Binding Name}" />
                                            <ContentControl Grid.Row="1" Content="{Binding Page}" />
                                        </Grid>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation
                                                                        Storyboard.TargetProperty="Opacity"
                                                                        From="0"
                                                                        To="1"
                                                                        Duration="0:0:0.4">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <CubicEase EasingMode="EaseOut" />
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation
                                                                        Storyboard.TargetProperty="Opacity"
                                                                        From="1"
                                                                        To="0"
                                                                        Duration="0:0:0.4">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <CubicEase EasingMode="EaseIn" />
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </Viewbox>
            </Grid>
            <StackPanel
                Grid.Row="2"
                Margin="10"
                HorizontalAlignment="Center"
                Orientation="Horizontal">

                <Button
                    Height="15"
                    Margin="10"
                    Command="{Binding PreviousCommand}"
                    Style="{StaticResource TransparentButton}">
                    <Path Fill="{DynamicResource Primary.Brush}" Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Data" Value="M658.29178 1003.504117l223.006914 0L398.119373 520.342595 881.298746 0 658.291782 0 175.112507 520.342595 658.29178 1003.504117z" />
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>
                <Button
                    Margin="10"
                    Command="{Binding ToggleAutoPlayCommand}"
                    Style="{StaticResource TransparentButton}">
                    <Path Fill="{DynamicResource Primary.Brush}" Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Data" Value="M8,5.14V19.14L19,12.14L8,5.14Z" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAutoPlaying}" Value="True">
                                        <Setter Property="Data" Value="M14,19H18V5H14M6,19H10V5H6V19Z" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>
                <Button
                    Height="15"
                    Margin="10"
                    Command="{Binding NextCommand}"
                    Style="{StaticResource TransparentButton}">
                    <Path Fill="{DynamicResource Primary.Brush}" Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Data" Value="M881.298746 520.342595 398.119373 0 175.112486 0 658.29178 520.342595 175.112507 1003.504117l223.006914 0L881.298746 520.342595z" />
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>
            </StackPanel>
        </Grid>

    </Grid>
</Page>
