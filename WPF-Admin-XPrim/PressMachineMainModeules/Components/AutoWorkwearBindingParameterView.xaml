<UserControl
    x:Class="PressMachineMainModeules.Components.AutoWorkwearBindingParameterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i18n="clr-namespace:WPF.Admin.Themes.I18n;assembly=WPF.Admin.Themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:AutoWorkwearCheckCodeViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel
            Grid.Row="0"
            Margin="10"
            HorizontalAlignment="Left">
            <Button BorderBrush="{DynamicResource Primary.Brush}" Command="{Binding GetConfigListCommand}">刷新配方列表</Button>
        </StackPanel>
        <Grid Grid.Row="1">
            <ItemsControl x:Name="itemControls" ItemsSource="{Binding AutoWorkwearBindingParameters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5">
                            <StackPanel Margin="5" Orientation="Horizontal">
                                <StackPanel.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="20" />
                                    </Style>

                                </StackPanel.Resources>
                                <TextBlock i18n:I18nLocalizeAttach.AutoLocalize="Workwear.WorkwearName"></TextBlock>
                                <TextBlock
                                    Width="300"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource Background.Secondary.Brush}"
                                    FontWeight="Bold"
                                    Text="{Binding WorkwearName}"
                                    TextAlignment="Center" />
                                <TextBlock Margin="20,0,0,0"
                                           i18n:I18nLocalizeAttach.AutoLocalize="Workwear.BindingParameter"></TextBlock>

                                <TextBlock
                                    x:Name="useNameText"
                                    Width="200"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource Background.Secondary.Brush}"
                                    FontWeight="Bold"
                                    Text="{Binding ParamerterName}"
                                    TextAlignment="Center" />
                                <ComboBox
                                    Name="selectComboBox"
                                    Width="200"
                                    Margin="10,0,0,0"
                                    ItemsSource="{Binding DataContext.PressMachineParameterList, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    SelectionChanged="ComboBox_SelectionChanged"
                                    Tag="{Binding}"
                                    Visibility="Hidden" />

                            </StackPanel>

                        </Border>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="selectComboBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>