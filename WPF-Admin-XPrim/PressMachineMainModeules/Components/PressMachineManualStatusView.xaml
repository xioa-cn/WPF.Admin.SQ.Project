<Window
    x:Class="PressMachineMainModeules.Components.PressMachineManualStatusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:PressMachineMainModeules.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="PressMachineManualStatusView"
    Width="280"
    Height="350"
    AllowsTransparency="True"
    Background="Transparent"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStyle="None"
    mc:Ignorable="d">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="50" GlassFrameThickness="1" />
    </WindowChrome.WindowChrome>

    <Border
        BorderBrush="{DynamicResource Primary.Brush}"
        BorderThickness="2"
        CornerRadius="8">
        <Grid>
            <Border
                Width="280"
                Height="330"
                Margin="5"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                CornerRadius="8"
                MouseLeftButtonDown="Border_MouseLeftButtonDown">
                <Viewbox>
                    <local:ManaulStateUserControl Margin="1" />
                </Viewbox>
            </Border>

            <Button
                Margin="5"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Click="Button_Click">
                退出
            </Button>
        </Grid>

    </Border>
</Window>
