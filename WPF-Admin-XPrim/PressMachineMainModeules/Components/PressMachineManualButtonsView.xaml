<UserControl
    x:Class="PressMachineMainModeules.Components.PressMachineManualButtonsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:attached="clr-namespace:WPF.Admin.Themes.Attached;assembly=WPF.Admin.Themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:PreManualViewModel}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="CircleCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border
                                x:Name="CheckBoxBorder"
                                Width="24"
                                Height="24"
                                Background="Transparent"
                                BorderBrush="#666666"
                                BorderThickness="2"
                                CornerRadius="12">
                                <Path
                                    x:Name="CheckMark"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="M6,12 L10,16 L18,8"
                                    Stroke="Green"
                                    StrokeEndLineCap="Round"
                                    StrokeStartLineCap="Round"
                                    StrokeThickness="2.5"
                                    Visibility="Hidden" />
                            </Border>
                            <ContentPresenter
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Content}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="Green" />
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#2196F3" />
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#1976D2" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#E3F2FD" />
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style
            x:Key="ManualButton"
            BasedOn="{StaticResource ButtonBaseStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="Green" />
            <Style.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="Red" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ABtn" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderBrush="Green" BorderThickness="1">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                                <ContentPresenter />
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style
            x:Key="ManualBlueButton"
            BasedOn="{StaticResource ButtonBaseStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="DeepSkyBlue" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Width" Value="300" />
            <Setter Property="FontSize" Value="25" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter Property="UIElement.Opacity" Value=".9" />
                                <Setter Property="Background" Value="LightGreen" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="UIElement.Opacity" Value=".1" />
                                <Setter TargetName="border" Property="Background" Value="Orange" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="UIElement.Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
        <Style
            x:Key="ManualGreenButton"
            BasedOn="{StaticResource ButtonBaseStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="GreenYellow" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Width" Value="300" />
            <Setter Property="FontSize" Value="25" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter Property="UIElement.Opacity" Value=".9" />
                                <Setter Property="Background" Value="LightGreen" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="UIElement.Opacity" Value=".1" />
                                <Setter TargetName="border" Property="Background" Value="DeepSkyBlue" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="UIElement.Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
        <Style
            x:Key="ManualRedButton"
            BasedOn="{StaticResource ButtonBaseStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="YellowGreen" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Width" Value="300" />
            <Setter Property="FontSize" Value="25" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter Property="UIElement.Opacity" Value=".9" />
                                <Setter Property="Background" Value="LightGreen" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="UIElement.Opacity" Value=".1" />
                                <Setter TargetName="border" Property="Background" Value="DeepSkyBlue" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="UIElement.Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </UserControl.Resources>
    <StackPanel
        x:Name="leftarea"
        HorizontalAlignment="Right"
        VerticalAlignment="Center">
        <Border
            Margin="10"
            Padding="20"
            Background="{DynamicResource Surface.Brush}"
            BorderBrush="{DynamicResource Border.Brush}"
            BorderThickness="1"
            CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="10"
                    Direction="315"
                    Opacity="0.1"
                    ShadowDepth="2" />
            </Border.Effect>

            <StackPanel>

                <UniformGrid Columns="2">

                    <GroupBox Margin="10" Header="电缸启用屏蔽">
                        <UniformGrid Columns="2">
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一启用屏蔽True}"
                                Style="{DynamicResource ManualBlueButton}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸启用"
                                        IsChecked="{Binding PressMachine01Status.电缸启用}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一启用屏蔽False}"
                                Style="{DynamicResource ManualBlueButton}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸屏蔽"
                                        IsChecked="{Binding PressMachine01Status.电缸屏蔽}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二启用屏蔽True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号电缸启用"
                                        IsChecked="{Binding PressMachine02Status.电缸启用}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>

                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二启用屏蔽False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content=" 二号电缸屏蔽"
                                        IsChecked="{Binding PressMachine02Status.电缸屏蔽}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三启用屏蔽True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸启用"
                                        IsChecked="{Binding PressMachine03Status.电缸启用}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三启用屏蔽False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸屏蔽"
                                        IsChecked="{Binding PressMachine03Status.电缸屏蔽}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                        </UniformGrid>
                    </GroupBox>

                    <GroupBox Margin="10" Header="手自动切换">
                        <UniformGrid Columns="2">

                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一手自动切换True}"
                                Style="{DynamicResource ManualBlueButton}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸手动"
                                        IsChecked="{Binding PressMachine01Status.手动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一手自动切换False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸自动"
                                        IsChecked="{Binding PressMachine01Status.自动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二手自动切换True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号电缸手动"
                                        IsChecked="{Binding PressMachine02Status.手动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二手自动切换False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号电缸自动"
                                        IsChecked="{Binding PressMachine02Status.自动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三手自动切换True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸手动"
                                        IsChecked="{Binding PressMachine03Status.手动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三手自动切换False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸自动"
                                        IsChecked="{Binding PressMachine03Status.自动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                        </UniformGrid>
                    </GroupBox>

                    <GroupBox Margin="10" Header="自动启动">
                        <UniformGrid Columns="2">
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一自动启动1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一自动启动1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号自动启动"
                                        IsChecked="{Binding PressMachine01Status.自动启动}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一自动停止1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一自动停止1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号自动停止"
                                        IsChecked="{Binding PressMachine01Status.自动停止}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二自动启动1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二自动启动1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号自动启动"
                                        IsChecked="{Binding PressMachine02Status.自动启动}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二自动停止1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二自动停止1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号自动停止"
                                        IsChecked="{Binding PressMachine02Status.自动停止}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三自动启动1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三自动启动1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号自动启动"
                                        IsChecked="{Binding PressMachine03Status.自动启动}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三自动停止1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三自动停止1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号自动停止"
                                        IsChecked="{Binding PressMachine03Status.自动停止}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                        </UniformGrid>
                    </GroupBox>
                    <GroupBox Margin="10" Header="压力测试">
                        <UniformGrid Columns="1">
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一压力测试1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一压力测试1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                一号压力测试
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二压力测试1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二压力测试1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                二号压力测试
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三压力测试1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三压力测试1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                三号压力测试
                            </Button>
                        </UniformGrid>
                    </GroupBox>
                    <!--<GroupBox Margin="10" Header="一号电缸">
                        <UniformGrid Columns="2">
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一启用屏蔽True}"
                                Style="{DynamicResource ManualBlueButton}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸启用"
                                        IsChecked="{Binding PressMachine01Status.电缸启用}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一启用屏蔽False}"
                                Style="{DynamicResource ManualBlueButton}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸屏蔽"
                                        IsChecked="{Binding PressMachine01Status.电缸屏蔽}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一手自动切换True}"
                                Style="{DynamicResource ManualBlueButton}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸手动"
                                        IsChecked="{Binding PressMachine01Status.手动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一手自动切换False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号电缸自动"
                                        IsChecked="{Binding PressMachine01Status.自动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一手动正转1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一手动正转1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                一号电缸正转
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一手动反转1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一手动反转1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                一号电缸反转
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一自动启动1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一自动启动1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号自动启动"
                                        IsChecked="{Binding PressMachine01Status.自动启动}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一自动停止1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一自动停止1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="一号自动停止"
                                        IsChecked="{Binding PressMachine01Status.自动停止}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸一压力测试1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸一压力测试1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                一号压力测试
                            </Button>
                        </UniformGrid>
                    </GroupBox>
                    <GroupBox Margin="10" Header="二号电缸">
                        <UniformGrid Columns="2">
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二启用屏蔽True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号电缸启用"
                                        IsChecked="{Binding PressMachine02Status.电缸启用}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>

                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二启用屏蔽False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content=" 二号电缸屏蔽"
                                        IsChecked="{Binding PressMachine02Status.电缸屏蔽}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二手自动切换True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号电缸手动"
                                        IsChecked="{Binding PressMachine02Status.手动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二手自动切换False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号电缸自动"
                                        IsChecked="{Binding PressMachine02Status.自动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二手动正转1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二手动正转1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                二号电缸正转
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二手动反转1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二手动反转1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                二号电缸反转
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二自动启动1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二自动启动1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号自动启动"
                                        IsChecked="{Binding PressMachine02Status.自动启动}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二自动停止1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二自动停止1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="二号自动停止"
                                        IsChecked="{Binding PressMachine02Status.自动停止}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸二压力测试1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸二压力测试1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                二号压力测试
                            </Button>
                        </UniformGrid>
                    </GroupBox>
                    <GroupBox Margin="10" Header="三号电缸">
                        <UniformGrid Columns="2">
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三启用屏蔽True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸启用"
                                        IsChecked="{Binding PressMachine03Status.电缸启用}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三启用屏蔽False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸屏蔽"
                                        IsChecked="{Binding PressMachine03Status.电缸屏蔽}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三手自动切换True}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸手动"
                                        IsChecked="{Binding PressMachine03Status.手动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三手自动切换False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号电缸自动"
                                        IsChecked="{Binding PressMachine03Status.自动模式}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三手动正转1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三手动正转1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                三号电缸正转
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三手动反转1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三手动反转1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                三号电缸反转
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三自动启动1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三自动启动1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号自动启动"
                                        IsChecked="{Binding PressMachine03Status.自动启动}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三自动停止1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三自动停止1False}"
                                Style="{DynamicResource ManualBlueButton}">

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Content="三号自动停止"
                                        IsChecked="{Binding PressMachine03Status.自动停止}"
                                        IsHitTestVisible="False"
                                        Style="{StaticResource CircleCheckBoxStyle}" />
                                </StackPanel>
                            </Button>
                            <Button
                                Margin="8"
                                attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding 电缸三压力测试1True}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding 电缸三压力测试1False}"
                                Style="{DynamicResource ManualBlueButton}">
                                三号压力测试
                            </Button>
                        </UniformGrid>
                    </GroupBox>-->



                    <GroupBox Margin="10" Header="压力补偿">
                        <UniformGrid Columns="2">
                            <StackPanel
                                Height="30"
                                Margin="0,10"
                                Orientation="Horizontal">
                                <TextBlock
                                    Width="120"
                                    VerticalAlignment="Center"
                                    TextAlignment="Center">
                                    一号正向补偿：
                                </TextBlock>
                                <TextBox Width="130" Text="{Binding ManualParameter01.正向压力补偿}" />
                                <StackPanel VerticalAlignment="Center">
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter01.AddCommand}"
                                        CommandParameter="正向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        +
                                    </Button>
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter01.CutdownCommand}"
                                        CommandParameter="正向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        <TextBlock FontWeight="Bold">-</TextBlock>
                                    </Button>
                                </StackPanel>
                                <Button
                                    Margin="10,0"
                                    Command="{Binding WriteParams1Command}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ManualButton}">
                                    写入
                                </Button>
                            </StackPanel>
                            <StackPanel
                                Height="30"
                                Margin="0,10"
                                Orientation="Horizontal">
                                <TextBlock
                                    Width="120"
                                    VerticalAlignment="Center"
                                    TextAlignment="Center">
                                    一号负向补偿：
                                </TextBlock>
                                <TextBox Width="130" Text="{Binding ManualParameter01.负向压力补偿}" />
                                <StackPanel VerticalAlignment="Center">
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter01.AddCommand}"
                                        CommandParameter="负向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        +
                                    </Button>
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter01.CutdownCommand}"
                                        CommandParameter="负向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        <TextBlock FontWeight="Bold">-</TextBlock>
                                    </Button>
                                </StackPanel>
                                <Button
                                    Margin="10,0"
                                    Command="{Binding WriteParams1Command}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ManualButton}">
                                    写入
                                </Button>
                            </StackPanel>

                            <StackPanel
                                Height="30"
                                Margin="0,10"
                                Orientation="Horizontal">
                                <TextBlock
                                    Width="120"
                                    VerticalAlignment="Center"
                                    TextAlignment="Center">
                                    二号正向补偿：
                                </TextBlock>
                                <TextBox Width="130" Text="{Binding ManualParameter02.正向压力补偿}" />
                                <StackPanel VerticalAlignment="Center">
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter02.AddCommand}"
                                        CommandParameter="正向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        +
                                    </Button>
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter02.CutdownCommand}"
                                        CommandParameter="正向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        <TextBlock FontWeight="Bold">-</TextBlock>
                                    </Button>
                                </StackPanel>
                                <Button
                                    Margin="10,0"
                                    Command="{Binding WriteParams2Command}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ManualButton}">
                                    写入
                                </Button>
                            </StackPanel>
                            <StackPanel
                                Height="30"
                                Margin="0,10"
                                Orientation="Horizontal">
                                <TextBlock
                                    Width="120"
                                    VerticalAlignment="Center"
                                    TextAlignment="Center">
                                    二号负向补偿：
                                </TextBlock>
                                <TextBox Width="130" Text="{Binding ManualParameter02.负向压力补偿}" />
                                <StackPanel VerticalAlignment="Center">
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter02.AddCommand}"
                                        CommandParameter="负向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        +
                                    </Button>
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter02.CutdownCommand}"
                                        CommandParameter="负向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        <TextBlock FontWeight="Bold">-</TextBlock>
                                    </Button>
                                </StackPanel>
                                <Button
                                    Margin="10,0"
                                    Command="{Binding WriteParams2Command}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ManualButton}">
                                    写入
                                </Button>
                            </StackPanel>

                            <StackPanel
                                Height="30"
                                Margin="0,10"
                                Orientation="Horizontal">
                                <TextBlock
                                    Width="120"
                                    VerticalAlignment="Center"
                                    TextAlignment="Center">
                                    三号正向补偿：
                                </TextBlock>
                                <TextBox Width="130" Text="{Binding ManualParameter03.正向压力补偿}" />
                                <StackPanel VerticalAlignment="Center">
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter03.AddCommand}"
                                        CommandParameter="正向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        +
                                    </Button>
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter03.CutdownCommand}"
                                        CommandParameter="正向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        <TextBlock FontWeight="Bold">-</TextBlock>
                                    </Button>
                                </StackPanel>
                                <Button
                                    Margin="10,0"
                                    Command="{Binding WriteParams3Command}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ManualButton}">
                                    写入
                                </Button>
                            </StackPanel>
                            <StackPanel
                                Height="30"
                                Margin="0,10"
                                Orientation="Horizontal">
                                <TextBlock
                                    Width="120"
                                    VerticalAlignment="Center"
                                    TextAlignment="Center">
                                    三号负向补偿：
                                </TextBlock>
                                <TextBox Width="130" Text="{Binding ManualParameter03.负向压力补偿}" />
                                <StackPanel VerticalAlignment="Center">
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter03.AddCommand}"
                                        CommandParameter="负向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        +
                                    </Button>
                                    <Button
                                        Width="20"
                                        Height="13"
                                        Command="{Binding ManualParameter03.CutdownCommand}"
                                        CommandParameter="负向压力补偿"
                                        Style="{StaticResource ABtn}">
                                        <TextBlock FontWeight="Bold">-</TextBlock>
                                    </Button>
                                </StackPanel>
                                <Button
                                    Margin="10,0"
                                    Command="{Binding WriteParams3Command}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ManualButton}">
                                    写入
                                </Button>
                            </StackPanel>
                        </UniformGrid>
                    </GroupBox>

                    <GroupBox Margin="10" Header="">

                        <UniformGrid Columns="2">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <StackPanel.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="20" />
                                    </Style>
                                </StackPanel.Resources>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Margin="10,0">一号压力：</TextBlock>
                                    <TextBlock
                                        Width="50"
                                        Foreground="Green"
                                        Text="{Binding MaualShowFloatData.压力1}" />
                                    <TextBlock Margin="10,0">一号频率：</TextBlock>
                                    <TextBlock
                                        Width="50"
                                        Foreground="Green"
                                        Text="{Binding MaualShowFloatData.频率1}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Margin="10,0">二号压力：</TextBlock>
                                    <TextBlock
                                        Width="50"
                                        Foreground="Green"
                                        Text="{Binding MaualShowFloatData.压力2}" />
                                    <TextBlock Margin="10,0">二号频率：</TextBlock>
                                    <TextBlock
                                        Width="50"
                                        Foreground="Green"
                                        Text="{Binding MaualShowFloatData.频率2}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Margin="10,0">三号压力：</TextBlock>
                                    <TextBlock
                                        Width="50"
                                        Foreground="Green"
                                        Text="{Binding MaualShowFloatData.压力3}" />
                                    <TextBlock Margin="10,0">三号频率：</TextBlock>
                                    <TextBlock
                                        Width="50"
                                        Foreground="Green"
                                        Text="{Binding MaualShowFloatData.频率3}" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Button
                                    Width="200"
                                    Margin="10"
                                    Command="{Binding OpenManualParamsWriteWindowCommand}"
                                    Style="{DynamicResource ManualGreenButton}">
                                    参数
                                </Button>
                                <Button
                                    Width="200"
                                    Margin="10"
                                    attached:MouseButtonBehavior.MouseLeftButtonDownBehavior="{Binding StatusWriteCommand}"
                                    attached:MouseButtonBehavior.MouseLeftButtonDownParameter="{Binding Invo报警复位True}"
                                    attached:MouseButtonBehavior.MouseLeftButtonUpBehavior="{Binding StatusWriteCommand}"
                                    attached:MouseButtonBehavior.MouseLeftButtonUpParameter="{Binding Invo报警复位False}"
                                    Style="{DynamicResource ManualRedButton}">
                                    报警复位
                                </Button>
                            </StackPanel>
                        </UniformGrid>
                    </GroupBox>
                </UniformGrid>




            </StackPanel>


        </Border>
    </StackPanel>
</UserControl>
