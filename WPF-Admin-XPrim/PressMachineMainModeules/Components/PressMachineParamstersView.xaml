<UserControl
    x:Class="PressMachineMainModeules.Components.PressMachineParamstersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="https://handyorg.github.io/handycontrol"
    xmlns:converter="clr-namespace:PressMachineMainModeules.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:PressMachineMainModeules.Helper"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:PressMachineMainModeules.Models"
    xmlns:utils="clr-namespace:PressMachineMainModeules.Utils"
    xmlns:val="clr-namespace:PressMachineMainModeules.Attributes"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    x:Name="preparams"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{DynamicResource Background.Brush}"
    mc:Ignorable="d">
    <UserControl.Resources>

        <converter:EnumToChineseConverter x:Key="enumtochinese" />
        <Style x:Key="CorBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
        </Style>

    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel>
            <GroupBox
                Margin="10"
                Background="{DynamicResource Surface.Brush}"
                FontWeight="Bold"
                Header="{Binding ElementName=preparams, Path=Title}">
                <UniformGrid MaxWidth="850" Columns="2">
                    <!--<Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">待机位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                    -->
                    <!--  使 TextBox 占满剩余空间  -->
                    <!--
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.待机位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_待机位置}" Min="{x:Static utils:PressParamsValidationValue.Min_待机位置}" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">待机速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                    -->
                    <!--  使 TextBox 占满剩余空间  -->
                    <!--
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.待机速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_待机速度}" Min="{x:Static utils:PressParamsValidationValue.Min_待机速度}" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}"  />
                            </Grid>
                        </StackPanel>
                    </Border>-->

                    <!--<Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">待机位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                    -->
                    <!--  使 TextBox 占满剩余空间  -->
                    <!--
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.待机位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_待机位置}" Min="{x:Static utils:PressParamsValidationValue.Min_待机位置}" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">待机速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                    -->
                    <!--  使 TextBox 占满剩余空间  -->
                    <!--
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.待机速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_待机速度}" Min="{x:Static utils:PressParamsValidationValue.Min_待机速度}" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}"  />
                            </Grid>
                        </StackPanel>
                    </Border>-->



                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第一位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第一位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第一位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第一位置}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第一速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第一速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第一速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第一速度}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>


                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第二位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第二位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第二位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第二位置}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第二速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第二速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第二速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第二速度}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>




                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第三位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第三位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第三位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第三位置}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第三速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第三速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第三速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第三速度}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>


                    <!--<Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第四位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                    -->
                    <!--  使 TextBox 占满剩余空间  -->
                    <!--
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第四位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第四位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第四位置}" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">第四速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                    -->
                    <!--  使 TextBox 占满剩余空间  -->
                    <!--
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.第四速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第四速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第四速度}" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}"  />
                            </Grid>
                        </StackPanel>
                    </Border>-->



                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">保护压力</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.保护压力"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_保护压力}" Min="{x:Static utils:PressParamsValidationValue.Min_保护压力}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PressUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>



                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">保压时间</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.保压时间"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_保压时间}" Min="{x:Static utils:PressParamsValidationValue.Min_保压时间}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="S" />
                            </Grid>
                        </StackPanel>
                    </Border>


                    <!--<Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压力补偿</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                    -->
                    <!--  使 TextBox 占满剩余空间  -->
                    <!--
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineParamsDa.压力补偿"
                                                UpdateSourceTrigger="LostFocus">
                                                <Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_压力补偿}" Min="{x:Static utils:PressParamsValidationValue.Min_压力补偿}" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}"  />
                            </Grid>
                        </StackPanel>
                    </Border>-->
                </UniformGrid>
            </GroupBox>

            <GroupBox
                Margin="10"
                Background="{DynamicResource Surface.Brush}"
                FontWeight="Bold"
                Header="曲线判断">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <DataGrid
                        x:Name="MonitorRec"
                        VerticalAlignment="Top"
                        AutoGenerateColumns="False"
                        Background="{DynamicResource Border.Brush}"
                        ItemsSource="{Binding MonitorRecs, Mode=TwoWay, ElementName=preparams}">
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="50"
                                Binding="{Binding Id}"
                                Header="ID"
                                IsReadOnly="True" />
                            <DataGridTextColumn
                                Width="100"
                                Binding="{Binding Name}"
                                Header="名称" />
                            <!--  DisplayMemberPath指定在 UI 界面上显示哪个属性值  -->
                            <DataGridComboBoxColumn
                                Width="240"
                                DisplayMemberPath="Description"
                                Header="类型"
                                ItemsSource="{Binding Source={helper:EnumBindingSource {x:Type model:MonitorType}}}"
                                SelectedValueBinding="{Binding MonitorType}"
                                SelectedValuePath="Value" />

                            <DataGridTextColumn
                                Width="125"
                                Binding="{Binding MinX}"
                                Header="位置下限" />

                            <DataGridTextColumn
                                Width="125"
                                Binding="{Binding MaxX}"
                                Header="位置上限" />

                            <DataGridTextColumn
                                Width="125"
                                Binding="{Binding MinY}"
                                Header="压力下限" />

                            <DataGridTextColumn
                                Width="125"
                                Binding="{Binding MaxY}"
                                Header="压力上限" />

                        </DataGrid.Columns>
                        <DataGrid.Resources>
                            <Style TargetType="ContentControl">
                                <Setter Property="Width" Value="100" />
                            </Style>
                        </DataGrid.Resources>
                    </DataGrid>


                    <DockPanel Grid.Row="1" HorizontalAlignment="Center">
                        <Button
                            Margin="10"
                            controls:BorderElement.CornerRadius="15"
                            Click="AddWindow_Click"
                            FontSize="15"
                            Style="{StaticResource ButtonPrimary}">
                            添加窗口
                        </Button>
                        <Button
                            Margin="10"
                            controls:BorderElement.CornerRadius="15"
                            Click="InsertWindow_Click"
                            FontSize="15"
                            Style="{StaticResource ButtonPrimary}">
                            插入窗口
                        </Button>
                        <Button
                            Margin="10"
                            controls:BorderElement.CornerRadius="15"
                            Click="DeleteWindow_Click"
                            FontSize="15"
                            Style="{StaticResource ButtonPrimary}">
                            删除窗口
                        </Button>
                        <Button
                            Margin="10"
                            controls:BorderElement.CornerRadius="15"
                            Click="ClearWindow_Click"
                            FontSize="15"
                            Style="{StaticResource ButtonPrimary}">
                            清空窗口
                        </Button>
                    </DockPanel>
                </Grid>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
