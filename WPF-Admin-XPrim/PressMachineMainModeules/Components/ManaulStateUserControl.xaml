<UserControl
    x:Class="PressMachineMainModeules.Components.ManaulStateUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:PressMachineMainModeules.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:PreManualViewModel}"
    FontSize="25"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converter:NoBooleanToVisiConverter x:Key="NoBooleanToVisi" />
        <converter:AlaramTestConverter x:Key="AlaramTestConverter" />
        <converter:ModeTestConverter x:Key="ModeTestConverter" />
        <converter:PlotTestConverter x:Key="PlotTestConverter" />
        <converter:AutoStateTestConverter x:Key="AutoStateTestConverter" />
        <converter:YPointTestConverter x:Key="YPointTestConverter" />
        <converter:AlarmTestColorConverter x:Key="AlarmTestColorConverter" />
    </UserControl.Resources>

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <UniformGrid Columns="2">
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}">
                    自动模式
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}"
                    IsHitTestVisible="False">
                    手动模式
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}">
                    一号启用
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}"
                    IsHitTestVisible="False">
                    二号启用
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}">
                    三号启用
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}"
                    IsHitTestVisible="False">
                    四号启用
                </TextBlock>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}">
                    一号启动
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}"
                    IsHitTestVisible="False">
                    二号启动
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}">
                    三号启动
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}"
                    IsHitTestVisible="False">
                    四号启动
                </TextBlock>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}">
                    一号停止
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}"
                    IsHitTestVisible="False">
                    二号停止
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}">
                    三号停止
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsHitTestVisible="False" Style="{StaticResource CheckBoxBaseStyle}" />
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Foreground="{DynamicResource Text.Primary.Brush}"
                    IsHitTestVisible="False">
                    四号停止
                </TextBlock>
            </StackPanel>

            <!--<TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                当前模式：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.自动模式, Converter={StaticResource ModeTestConverter}}" />

            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                自动状态：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.自动状态, Converter={StaticResource AutoStateTestConverter}}" />

            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                压机原点：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.压机原点, Converter={StaticResource YPointTestConverter}}" />

            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴原点：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.X轴原点, Converter={StaticResource YPointTestConverter}}" />


            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                压机待机位：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.压机待机位, Converter={StaticResource YPointTestConverter}}" />


            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴待机位：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.X轴待机位, Converter={StaticResource YPointTestConverter}}" />


            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴一次待机位：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.X轴一次待机位, Converter={StaticResource YPointTestConverter}}" />


            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴压装位一：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.X轴压装位置一, Converter={StaticResource YPointTestConverter}}" />


            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴压装位二：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.X轴压装位置二, Converter={StaticResource YPointTestConverter}}" />

            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴压装位三：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.X轴压装位置三, Converter={StaticResource YPointTestConverter}}" />


            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴压装位四：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Primary.Brush}"
                Text="{Binding IoPoint.X轴压装位置四, Converter={StaticResource YPointTestConverter}}" />-->


            <!--<TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴位置指示：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Success.Brush}"
                Text="{Binding IoPoint.X轴位置指示}" />



            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                X轴手动速度：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Success.Brush}"
                Text="{Binding IoPoint.X轴手动速度}" />-->



            <!--<TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                压机位置指示：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Success.Brush}"
                Text="{Binding IoPoint.压机位置指示}" />-->

            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                压机压力指示：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Success.Brush}"
                Text="{Binding IoPoint.压机压力指示}" />


            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                压机速度指示：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Success.Brush}"
                Text="{Binding IoPoint.手动速度}" />






            <TextBlock
                Margin="1"
                HorizontalAlignment="Left"
                FontWeight="Bold"
                Foreground="{DynamicResource Text.Primary.Brush}">
                手动保护压力：
            </TextBlock>
            <TextBlock
                Margin="1"
                Foreground="{DynamicResource Success.Brush}"
                Text="{Binding IoPoint.手动保护}" />

        </UniformGrid>
    </StackPanel>

</UserControl>
