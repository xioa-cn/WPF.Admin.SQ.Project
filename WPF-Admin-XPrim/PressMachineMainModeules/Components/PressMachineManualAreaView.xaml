<UserControl
    x:Class="PressMachineMainModeules.Components.PressMachineManualAreaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:com="clr-namespace:PressMachineMainModeules.Components"
    xmlns:converter="clr-namespace:PressMachineMainModeules.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:PreManualViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converter:NoBooleanToVisiConverter x:Key="NoBooleanToVisi" />
        <converter:AlaramTestConverter x:Key="AlaramTestConverter" />
        <converter:ModeTestConverter x:Key="ModeTestConverter" />
        <converter:PlotTestConverter x:Key="PlotTestConverter" />
        <converter:AutoStateTestConverter x:Key="AutoStateTestConverter" />
        <converter:YPointTestConverter x:Key="YPointTestConverter" />
        <converter:AlarmTestColorConverter x:Key="AlarmTestColorConverter" />
        <Style TargetType="Button">
            <Setter Property="Width" Value="300" />
        </Style>
    </UserControl.Resources>

    <Viewbox Stretch="Uniform">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width=".8*" />
            </Grid.ColumnDefinitions>
            <Border Margin="10">
                <local:PressMachineManualButtonsView />
            </Border>




            <!--<Border Grid.Column="1" Margin="10">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border
                        Margin="10"
                        Padding="20"
                        Background="{DynamicResource Surface.Brush}"
                        BorderBrush="{DynamicResource Border.Brush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <Border.Effect>
                            <DropShadowEffect
                                BlurRadius="10"
                                Direction="315"
                                Opacity="0.1"
                                ShadowDepth="2" />
                        </Border.Effect>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                            <TextBlock Visibility="{Binding StatusWindowShow, Converter={StaticResource BooleanToVisibilityConverter}}">状态栏已弹出</TextBlock>
                            <local:ManaulStateUserControl Visibility="{Binding StatusWindowShow, Converter={StaticResource NoBooleanToVisi}}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>-->
        </Grid>
    </Viewbox>
</UserControl>