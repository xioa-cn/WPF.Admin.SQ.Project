<UserControl
    x:Class="PressMachineMainModeules.Components.AutoBarcodeCharacteristicsContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i18n="clr-namespace:WPF.Admin.Themes.I18n;assembly=WPF.Admin.Themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Name="BarcodeCharacteristics"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Margin="10" Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" FontSize="15"
                       Text="{i18n:Localize BarcodeCharacteristics.CheckCode}">
            </TextBlock>
            <CheckBox VerticalAlignment="Center"
                      IsChecked="{Binding ElementName=BarcodeCharacteristics, Path=CheckRolaValueSetp.Open}" />
        </StackPanel>
        <Grid Grid.Row="1">
            <ScrollViewer>
                <Border
                    Margin="5"
                    BorderBrush="{DynamicResource Border.Brush}"
                    BorderThickness="4">
                    <StackPanel Margin="0,5">
                        <Border Margin="5,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="15"
                                    Text="{i18n:Localize BarcodeCharacteristics.MainCodeRule}" />
                                <TextBox
                                    Width="200"
                                    Margin="5,0"
                                    VerticalAlignment="Center"
                                    FontSize="15"
                                    Text="{Binding ElementName=BarcodeCharacteristics, Path=CheckRolaValueSetp.MainCodeRola}" />
                            </StackPanel>
                        </Border>
                        <Border
                            Height="5"
                            Margin="0,5"
                            BorderBrush="{DynamicResource Border.Brush}"
                            BorderThickness="3" />
                        <Button
                            Margin="5"
                            HorizontalAlignment="Left"
                            Content="{i18n:Localize BarcodeCharacteristics.AddParkCode}"
                            BorderBrush="{DynamicResource Primary.Brush}"
                            Click="AddRolaClick">
                        </Button>
                        <Grid Grid.Row="1">
                            <DataGrid
                                Width="auto"
                                Margin="10"
                                AlternatingRowBackground="{DynamicResource Surface.Brush}"
                                AutoGenerateColumns="False"
                                Background="{DynamicResource Background.Brush}"
                                BorderBrush="{DynamicResource Border.Brush}"
                                BorderThickness="1"
                                FontSize="20"
                                GridLinesVisibility="All"
                                HeadersVisibility="Column"
                                HorizontalGridLinesBrush="{DynamicResource Border.Brush}"
                                IsReadOnly="True"
                                ItemsSource="{Binding ElementName=BarcodeCharacteristics, Path=CheckRolaValueSetp.CheckRolaValues}"
                                LoadingRow="DataGrid_LoadingRow"
                                RowBackground="{DynamicResource Background.Brush}"
                                RowHeaderWidth="40"
                                VerticalGridLinesBrush="{DynamicResource Border.Brush}">

                                <DataGrid.Columns>

                                    <DataGridTextColumn
                                        Width="auto"
                                        Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Header}"
                                        Header="{i18n:Localize Table.Serial}"
                                        IsReadOnly="True" />

                                    <DataGridTextColumn
                                        Width="*"
                                        Binding="{Binding RolaString}"
                                        Header="{i18n:Localize BarcodeCharacteristics.ParkCodeRule}">
                                        <DataGridTextColumn.CellStyle>
                                            <Style BasedOn="{StaticResource cell}" TargetType="DataGridCell">
                                                <Setter Property="ToolTip">
                                                    <Setter.Value>
                                                        <ToolTip>
                                                            <StackPanel>
                                                                <TextBlock FontWeight="Bold"
                                                                           i18n:I18nLocalizeAttach.AutoLocalize="Table.ColumnName" />
                                                                <TextBlock i18n:I18nLocalizeAttach.AutoLocalize="BarcodeCharacteristics.ParkCodeRule"/>
                                                                <TextBlock FontWeight="Bold"
                                                                           i18n:I18nLocalizeAttach.AutoLocalize="Table.ColumnValue" />
                                                                <TextBlock Text="{Binding RolaString}" />
                                                            </StackPanel>
                                                        </ToolTip>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </DataGridTextColumn.CellStyle>
                                    </DataGridTextColumn>

                                    <!--  操作列  -->
                                    <DataGridTemplateColumn Width="120" Header="{i18n:Localize Table.Operation}">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button
                                                        Width="80"
                                                        Height="30"
                                                        Margin="5,0"
                                                        Background="{DynamicResource Primary.Brush}"
                                                        Click="DeleteSelectValueClick"
                                                        i18n:I18nLocalizeAttach.AutoLocalize="btn.Delete"
                                                        Foreground="{DynamicResource Text.Primary.Brush}"
                                                        Style="{StaticResource ButtonDanger}"
                                                        Tag="{Binding}" />

                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>