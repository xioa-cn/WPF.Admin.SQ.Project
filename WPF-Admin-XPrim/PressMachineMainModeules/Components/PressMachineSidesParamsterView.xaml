<UserControl
    x:Class="PressMachineMainModeules.Components.PressMachineSidesParamsterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:PressMachineMainModeules.Utils"
    xmlns:val="clr-namespace:PressMachineMainModeules.Attributes"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    Name="preparams"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="CorBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
        </Style>
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel>
            <GroupBox
                Margin="10"
                Background="{DynamicResource Surface.Brush}"
                FontWeight="Bold"
                Header="{Binding ElementName=preparams, Path=Title}">
                <UniformGrid MaxWidth="850" Columns="2">
                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">待机位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.待机位置"
                                                UpdateSourceTrigger="LostFocus" />
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">待机速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.待机速度"
                                                UpdateSourceTrigger="LostFocus" />
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装一位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第一位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第一位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第一位置}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装一速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第一速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第一速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第一速度}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装二位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第二位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第二位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第二位置}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装二速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第二速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第二速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第二速度}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装三位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第三位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第三位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第三位置}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装三速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第三速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                                    <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第三速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第三速度}" />
                                                </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border
                        MinWidth="200"
                        MaxWidth="400"
                        Margin="20,10,10,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装四位置</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第四位置"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                  <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第三位置}" Min="{x:Static utils:PressParamsValidationValue.Min_第三位置}" />
                              </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=PositionUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border
                        MaxWidth="400"
                        Margin="10,10,20,10"
                        Background="{DynamicResource Border.Brush}"
                        Style="{StaticResource CorBorder}">
                        <StackPanel Margin="10">
                            <TextBlock FontWeight="Bold">压装四速度</TextBlock>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  使 TextBox 占满剩余空间  -->
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="0,0,0,0">
                                    <TextBox
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontWeight="Bold"
                                        Style="{StaticResource TextBoxExtend}">
                                        <TextBox.Text>
                                            <Binding
                                                ElementName="preparams"
                                                Path="PressMachineSideswayDa.第四速度"
                                                UpdateSourceTrigger="LostFocus">
                                                <!--<Binding.ValidationRules>
                                  <val:FloatRule Max="{x:Static utils:PressParamsValidationValue.Max_第三速度}" Min="{x:Static utils:PressParamsValidationValue.Min_第三速度}" />
                              </Binding.ValidationRules>-->
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </Border>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="5,0"
                                    VerticalAlignment="Bottom"
                                    FontWeight="Bold"
                                    Text="{Binding Source={x:Static vm:GlobalUnitViewModel.Insance}, Path=SpeedUnitName}" />
                            </Grid>
                        </StackPanel>
                    </Border>

                </UniformGrid>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
