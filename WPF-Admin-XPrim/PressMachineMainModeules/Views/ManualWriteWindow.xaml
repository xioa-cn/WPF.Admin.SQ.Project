<Window
    x:Class="PressMachineMainModeules.Views.ManualWriteWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    Title="手动参数集"
    Width="1125"
    Height="650"
    d:DataContext="{d:DesignInstance Type=vm:PreManualViewModel}"
    FontSize="20"
    ResizeMode="NoResize"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <Style
            x:Key="ManualButton"
            BasedOn="{StaticResource ButtonBaseStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="Green" />
            <Style.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="Red" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ABtn" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderBrush="Green" BorderThickness="1">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                                <ContentPresenter />
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border>
        <Grid>
            <StackPanel Orientation="Horizontal">
                <GroupBox
                    Width="350"
                    Margin="10"
                    Header="一号手动参数集">
                    <StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">手动速度：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.手动速度}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="手动速度"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">保护压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.手动保护压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="手动保护压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>

                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">设定频率：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.设定频率}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="设定频率"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>


                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">设定速度：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.设定速度}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="设定速度"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">对应扭矩：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.每千克对应扭矩}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="每千克对应扭矩"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">压力浮动：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.压力浮动范围}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="压力浮动范围"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">正向压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.设定正向压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="设定正向压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">正向补偿：</TextBlock>
                            <TextBox Width="130" Text="{Binding ManualParameter01.正向压力补偿}" />
                            <StackPanel VerticalAlignment="Center">
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter01.AddCommand}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    +
                                </Button>
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter01.CutdownCommand}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    <TextBlock FontWeight="Bold">-</TextBlock>
                                </Button>
                            </StackPanel>
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="正向压力补偿"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">负向压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter01.设定负向压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="设定负向压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">负向补偿：</TextBlock>
                            <TextBox Width="130" Text="{Binding ManualParameter01.负向压力补偿}" />
                            <StackPanel VerticalAlignment="Center">
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter01.AddCommand}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    +
                                </Button>
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter01.CutdownCommand}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    <TextBlock FontWeight="Bold">-</TextBlock>
                                </Button>
                            </StackPanel>
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams1Command}"
                                CommandParameter="负向压力补偿"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
                <GroupBox
                    Width="350"
                    Margin="10"
                    Header="二号手动参数集">
                    <StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">手动速度：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.手动速度}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="手动速度"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">保护压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.手动保护压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="手动保护压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>


                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">设定频率：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.设定频率}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="设定频率"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>


                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">设定速度：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.设定速度}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="设定速度"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>


                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">对应扭矩：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.每千克对应扭矩}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="每千克对应扭矩"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">压力浮动：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.压力浮动范围}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="压力浮动范围"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">正向压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.设定正向压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="设定正向压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">正向补偿：</TextBlock>
                            <TextBox Width="130" Text="{Binding ManualParameter02.正向压力补偿}" />
                            <StackPanel VerticalAlignment="Center">
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter02.AddCommand}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    +
                                </Button>
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter02.CutdownCommand}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    <TextBlock FontWeight="Bold">-</TextBlock>
                                </Button>
                            </StackPanel>
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="正向压力补偿"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">负向压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter02.设定负向压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="设定负向压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">负向补偿：</TextBlock>
                            <TextBox Width="130" Text="{Binding ManualParameter02.负向压力补偿}" />
                            <StackPanel VerticalAlignment="Center">
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter02.AddCommand}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    +
                                </Button>
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter02.CutdownCommand}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    <TextBlock FontWeight="Bold">-</TextBlock>
                                </Button>
                            </StackPanel>
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams2Command}"
                                CommandParameter="负向压力补偿"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
                <GroupBox
                    Width="350"
                    Margin="10"
                    Header="三号手动参数集">
                    <StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">手动速度：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.手动速度}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="手动速度"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">保护压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.手动保护压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="手动保护压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">设定频率：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.设定频率}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="设定频率"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>


                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">设定速度：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.设定速度}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="设定速度"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>


                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">对应扭矩：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.每千克对应扭矩}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="每千克对应扭矩"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">压力浮动：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.压力浮动范围}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="压力浮动范围"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">正向压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.设定正向压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="设定正向压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">正向补偿：</TextBlock>
                            <TextBox Width="130" Text="{Binding ManualParameter03.正向压力补偿}" />
                            <StackPanel VerticalAlignment="Center">
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter03.AddCommand}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    +
                                </Button>
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter03.CutdownCommand}"
                                    CommandParameter="正向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    <TextBlock FontWeight="Bold">-</TextBlock>
                                </Button>
                            </StackPanel>
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="正向压力补偿"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">负向压力：</TextBlock>
                            <TextBox Width="150" Text="{Binding ManualParameter03.设定负向压力}" />
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="设定负向压力"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                        <StackPanel
                            Height="30"
                            Margin="0,10"
                            Orientation="Horizontal">
                            <TextBlock Width="120" TextAlignment="Center">负向补偿：</TextBlock>
                            <TextBox Width="130" Text="{Binding ManualParameter03.负向压力补偿}" />
                            <StackPanel VerticalAlignment="Center">
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter03.AddCommand}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    +
                                </Button>
                                <Button
                                    Width="20"
                                    Height="13"
                                    Command="{Binding ManualParameter03.CutdownCommand}"
                                    CommandParameter="负向压力补偿"
                                    Style="{StaticResource ABtn}">
                                    <TextBlock FontWeight="Bold">-</TextBlock>
                                </Button>
                            </StackPanel>
                            <Button
                                Margin="10,0"
                                Command="{Binding WriteParams3Command}"
                                CommandParameter="负向压力补偿"
                                Style="{StaticResource ManualButton}">
                                写入
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
            <Button
                Margin="20"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Command="{Binding ReadParamsViewCommand}">
                读取参数
            </Button>
        </Grid>
    </Border>
</Window>
