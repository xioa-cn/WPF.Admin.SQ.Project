<Page
    x:Class="PressMachineMainModeules.Views.AutoParameterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:com="clr-namespace:PressMachineMainModeules.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i18n="clr-namespace:WPF.Admin.Themes.I18n;assembly=WPF.Admin.Themes"
    xmlns:local="clr-namespace:PressMachineMainModeules.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:PressMachineMainModeules.ViewModels"
    Title="AutoParameterPage"
    xmlns:converter="clr-namespace:PressMachineMainModeules.Converters"
    d:DataContext="{d:DesignInstance Type=viewmodels:AutoParameterViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Page.Resources>
        <converter:ParameterHeaderI18nConverter x:Key="HeaderI18NConverter" />
    </Page.Resources>
    <hc:DialogContainer>
        <Border
            Margin="1"
            BorderBrush="{DynamicResource Border.Brush}"
            BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <TabControl Margin="0,0,5,0" ItemsSource="{Binding AutoParameterModel.AutoParameterContents}">
                        <TabControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock
                                    i18n:I18nLocalizeAttach.AutoLocalize="{Binding Key,Converter={StaticResource HeaderI18NConverter}}" />
                            </DataTemplate>
                        </TabControl.ItemTemplate>
                        <TabControl.ContentTemplate>
                            <DataTemplate>
                                <Grid>
                                    <com:AutoParametersContentView
                                        Title="{Binding Key}"
                                        AutoMode="{Binding AutoMode}"
                                        ContentParameter="{Binding Instance}" />
                                </Grid>
                            </DataTemplate>
                        </TabControl.ContentTemplate>
                    </TabControl>

                </Grid>

                <GridSplitter
                    Grid.Column="1"
                    Width="3"
                    HorizontalAlignment="Left" />

                <Grid
                    Grid.Column="1"
                    Width="200"
                    Margin="5,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Margin="3" FontSize="15" Text="{i18n:Localize Parameter.Name}"></TextBlock>
                        <StackPanel Orientation="Horizontal">
                            <TextBox
                                x:Name="paramsName"
                                Width="144"
                                Style="{StaticResource TextBox.Small}"
                                Text="{Binding ConfigName}" />
                            <Button
                                Width="50"
                                Margin="3,0"
                                Content="{i18n:Localize btn.Search}"
                                Command="{Binding SearchCommand}"
                                Style="{StaticResource ButtonDefault}">

                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <Grid Grid.Row="1">
                        <ListBox
                            x:Name="ConfigNames"
                            Margin="0,5"
                            ItemsSource="{Binding ConfigNames}" />
                    </Grid>


                    <UniformGrid Grid.Row="2" Columns="2">
                        <Button
                            Width="80"
                            Content="{i18n:Localize btn.Parameter.Save}"
                            Margin="10,5"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource ButtonSuccess}">

                        </Button>
                        <Button
                            Width="80"
                            Content="{i18n:Localize btn.Parameter.ReadLocal}"
                            Command="{Binding ReadCommand}"
                            CommandParameter="{Binding ElementName=ConfigNames}"
                            Style="{StaticResource ButtonInfo}">

                        </Button>
                        <Button
                            Width="80"
                            Content="{i18n:Localize btn.Parameter.DeleteLocal}"
                            Command="{Binding DeleteCommand}"
                            CommandParameter="{Binding ElementName=ConfigNames}"
                            Style="{StaticResource ButtonDanger}">

                        </Button>
                        <Button
                            Width="80"
                            Content="{i18n:Localize btn.Parameter.UseLocal}"
                            Command="{Binding UseParamsterCommand}"
                            Style="{StaticResource ButtonSuccess}">

                        </Button>
                        <Button
                            Width="80"
                            Content="{i18n:Localize btn.Parameter.ReadPlc}"
                            Command="{Binding ReadPlcContentCommand}"
                            Style="{StaticResource ButtonInfo}">
                        </Button>
                    </UniformGrid>
                </Grid>

            </Grid>
        </Border>
    </hc:DialogContainer>
</Page>