<Page
    x:Class="PressMachineMainModeules.Views.PreSystemManager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i18n="clr-namespace:WPF.Admin.Themes.I18n;assembly=WPF.Admin.Themes"
    xmlns:auth="clr-namespace:WPF.Admin.Themes.Converter;assembly=WPF.Admin.Themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PressMachineMainModeules.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:vm="clr-namespace:PressMachineMainModeules.ViewModels"
    Title="PreSystemManager"
    d:DataContext="{d:DesignInstance Type=vm:PreSystemManagerViewModel}"
    IsEnabled="{Binding Source={x:Static auth:LoginAuthHelper.LoginUser}, Path=LoginAuth, Converter={StaticResource EngineerEnabled}}"
    mc:Ignorable="d">
    <hc:DialogContainer>
        <ScrollViewer>
            <StackPanel
                Margin="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <GroupBox
                    Padding="10"
                    BorderBrush="{DynamicResource Background.Secondary.Brush}"
                    FontWeight="Bold"
                    Header="{i18n:Localize PreSystem.SystemSetting}">
                    <StackPanel>
                        <!--  IP地址  -->
                        <!-- <DockPanel Margin="0,5"> -->
                        <!--  <TextBlock  -->
                        <!--  Width="80"  -->
                        <!--  VerticalAlignment="Center"  -->
                        <!--  Text="IP地址:" />  -->
                        <!--     <TextBox Height="30" Text="{Binding SystemConfigModel.Ip01, UpdateSourceTrigger=PropertyChanged}" /> -->
                        <!-- </DockPanel> -->

                        <!-- <DockPanel Margin="0,5"> -->
                        <!--  <TextBlock  -->
                        <!--  Width="80"  -->
                        <!--  VerticalAlignment="Center"  -->
                        <!--  Text="IP地址:" />  -->
                        <!--     <TextBox Height="30" Text="{Binding SystemConfigModel.Ip02, UpdateSourceTrigger=PropertyChanged}" /> -->
                        <!-- </DockPanel> -->
                        <!-- <DockPanel Margin="0,5"> -->
                        <!--  <TextBlock  -->
                        <!--  Width="80"  -->
                        <!--  VerticalAlignment="Center"  -->
                        <!--  Text="IP地址:" />  -->
                        <!--     <TextBox Height="30" Text="{Binding SystemConfigModel.Ip03, UpdateSourceTrigger=PropertyChanged}" /> -->
                        <!-- </DockPanel> -->

                        <!--  COM端口  -->
                        <!-- <DockPanel Margin="0,5"> -->
                        <!--  <TextBlock  -->
                        <!--  Width="80"  -->
                        <!--  VerticalAlignment="Center"  -->
                        <!--  Text="COM端口:" />  -->
                        <!--     <TextBox Height="30" Text="{Binding SystemConfigModel.Com, UpdateSourceTrigger=PropertyChanged}" /> -->
                        <!-- </DockPanel> -->


                        <!--  压力补偿  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="压力补偿:" />
                        <TextBox Height="30" Text="{Binding SystemConfigModel.压机传感器补偿, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>


                    -->
                        <!--  防护门屏蔽  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="防护门屏蔽:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.DoorStatus}" />
                    </DockPanel>

                    -->
                        <!--  光幕屏蔽  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="光幕屏蔽:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.LightCurtainStatus}" />
                    </DockPanel>

                    -->
                        <!--  蜂鸣器屏蔽  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="蜂鸣器屏蔽:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.BuzzerStatus}" />
                    </DockPanel>

                    -->
                        <!--  扫码屏蔽  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="扫码屏蔽:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.CodeStatus}" />
                    </DockPanel>

                    -->
                        <!--  结果屏蔽  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="PC结果屏蔽:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.PCResultStatus}" />
                    </DockPanel>

                    -->
                        <!--  NG续压  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="NG续压:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.NGConStatus}" />
                    </DockPanel>


                    -->
                        <!--  NG续压  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
         Width="80"
         VerticalAlignment="Center"
         Text="安全互锁屏蔽:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.安全互锁屏蔽}" />
                    </DockPanel>


                    -->
                        <!--  远程本地控制  -->
                        <!--
                    <DockPanel Margin="0,5">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="远程本地控制:" />
                        <CheckBox Height="30" IsChecked="{Binding SystemConfigModel.InterStatus}" />
                    </DockPanel>-->


                        <!--  数据文件夹  -->
                        <DockPanel Margin="0,5">
                            <TextBlock
                                Width="80"
                                VerticalAlignment="Center"
                                Text="{i18n:Localize PreSystem.DataFile}" />
                            <Button
                                Width="60"
                                Height="30"
                                Margin="5,0,0,0"
                                BorderBrush="{DynamicResource Primary.Brush}"
                                Command="{Binding BrowseFolderCommand}"
                                Content="{i18n:Localize PreSystem.Browse}"
                                DockPanel.Dock="Right" />
                            <TextBox
                                Height="30"
                                IsHitTestVisible="False"
                                Text="{Binding SystemConfigModel.DataFolder, UpdateSourceTrigger=PropertyChanged}" />
                        </DockPanel>

                        <DockPanel Margin="0,5">
                            <Button
                                Height="30"
                                HorizontalAlignment="Right"
                                Content="{i18n:Localize PreSystem.InitializedPressMachineDb}"
                                BorderBrush="{DynamicResource Primary.Brush}"
                                Command="{Binding InitializedPressMachineDbCommand}">

                            </Button>
                        </DockPanel>
                    </StackPanel>
                </GroupBox>

                <!--  按钮区域  -->
                <StackPanel
                    Margin="0,20,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        Width="80"
                        Height="30"
                        BorderBrush="{DynamicResource Primary.Brush}"
                        Command="{Binding SaveCommand}"
                        Content="{i18n:Localize btn.Save}"
                        FontWeight="Bold" />

                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </hc:DialogContainer>
</Page>