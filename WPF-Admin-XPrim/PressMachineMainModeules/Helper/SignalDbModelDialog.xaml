<UserControl
    x:Class="PressMachineMainModeules.Helper.SignalDbModelDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i18n="clr-namespace:WPF.Admin.Themes.I18n;assembly=WPF.Admin.Themes"
    xmlns:local="clr-namespace:PressMachineMainModeules.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:SignalDbModelDialogViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Border
        Width="300"
        Height="300"
        CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                Margin="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Border
                    Grid.Row="0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top">
                    <TextBlock Text="{Binding Title, FallbackValue=Title}" TextWrapping="Wrap" />
                </Border>
                <StackPanel Grid.Row="1" Margin="0,10">
                    <TextBlock VerticalAlignment="Center" Text="{i18n:Localize Signal.Code}" />
                    <TextBox
                        Width="200"
                        VerticalAlignment="Center"
                        Text="{Binding SignalDbModel.Code}" />
                    <TextBlock VerticalAlignment="Center" Text="{i18n:Localize Signal.Plc}" />
                    <TextBox
                        Width="200"
                        VerticalAlignment="Center"
                        Text="{Binding SignalDbModel.Plc}" />
                    <TextBlock VerticalAlignment="Center" Text="{i18n:Localize Signal.Position}" />
                    <TextBox
                        Width="200"
                        VerticalAlignment="Center"
                        Text="{Binding SignalDbModel.Position}" />
                    <TextBlock VerticalAlignment="Center" Text="{i18n:Localize Signal.value}" />
                    <TextBox
                        Width="200"
                        VerticalAlignment="Center"
                        Text="{Binding SignalDbModel.Value}" />
                </StackPanel>
            </Grid>
            <StackPanel
                Grid.Row="1"
                Margin="20,10,20,20"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Orientation="Horizontal">
                <ItemsControl ItemsSource="{Binding Buttons}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button
                                Margin="5"
                                Command="{Binding Command}"
                                Content="{Binding Text}"
                                Style="{StaticResource ButtonSuccess}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
